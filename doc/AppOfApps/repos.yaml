repos:
  hess.pm:
    base-chart-app.yaml
      version = main
     - values.yaml:
  michael.pm:
    base-chart-app.yaml
      version = 2.0.0
    - values.yaml:
      apps:
        immich:
          enabled: true
          version: 1.9

  iac:
    apps:
      generic:
        template/:
          generic-deployment.yaml
        Chart.yaml
        values.yaml
      base-chart:
        teamplate/:
          immich-app.yaml <-- version = ~
          sftpgo-app.yaml
          plex-app.yaml
        Chart.yaml
        values.yaml
      immich:
        template/:
          immich-deployment.yaml
        Chart.yaml
        values.yaml
      sftpgo:
        template/:
          sftpgo-deployment.yaml
        Chart.yaml
        values.yaml


values.yaml of immich app

enabled: true
gernic:
  deployment: true



values.yaml of base-chart


immich:
  enabled: true
  version: 1.0.0
  importJob:
      enabled: true
  generic:
    deployment: true


