apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: application-set
  namespace: argocd
spec:
  goTemplate: true
  goTemplateOptions: ["missingkey=error"]
  generators:
    - git:
        repoURL: 'https://github.com/JonasHess/homelab-iac.git'
        revision: feature/setup2
        directories:
          - path: 'environments/hess.pm/apps/*'
  template:
    metadata:
      name: 'app-{{.path.basenameNormalized}}'
      namespace: argocd
      finalizers:
        - resources-finalizer.argocd.argoproj.io
    spec:
      project: default
      sources:
        - repoURL: 'https://github.com/JonasHess/homelab-iac.git'
          targetRevision: feature/setup2
          path: base-chart
          helm:
            parameters:
              - name: 'apps.{{.path.basenameNormalized}}.enabled'
                value: "true"
            valueFiles:
              - $homelab-iac/{{.path.path}}/../../environment.yaml
              - $homelab-iac/{{.path.path}}/values.yaml
        - repoURL: 'https://github.com/JonasHess/homelab-iac.git'
          targetRevision: feature/setup2
          ref: homelab-iac
      destination:
        server: https://kubernetes.default.svc
      syncPolicy:
        automated: {}
