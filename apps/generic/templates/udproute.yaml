{{- $appName := required "appName must be set in values.yaml" .Values.appName }}

{{- if and .Values.ingress .Values.ingress.udp }}
  {{- $gatewayName := required "global.gateway.name must be set" $.Values.global.gateway.name }}
  {{- $gatewayNamespace := required "global.gateway.namespace must be set" $.Values.global.gateway.namespace }}
  {{- range $ingress := .Values.ingress.udp }}
---
apiVersion: gateway.networking.k8s.io/v1alpha2
kind: UDPRoute
metadata:
  name: {{ $appName }}-udp-{{ required "port must be specified for each UDP ingress" $ingress.port }}
  namespace: {{ $.Release.Namespace }}
  annotations:
    argocd.argoproj.io/sync-options: Prune=true,SkipDryRunOnMissingResource=true
spec:
  parentRefs:
    - name: {{ $gatewayName }}
      namespace: {{ $gatewayNamespace }}
      sectionName: {{ required "listener must be specified for each UDP ingress" $ingress.listener }}
  rules:
    - backendRefs:
        - name: {{ $appName }}-service
          port: {{ $ingress.port }}
  {{- end }}
{{- end }}
