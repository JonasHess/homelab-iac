{{- if .Values.configMaps }}
  {{- range $cmName, $cmData := .Values.configMaps }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ $cmName }}
  namespace: {{ $.Release.Namespace }}
data:
{{- tpl (toYaml $cmData) $ | nindent 2 }}
  {{- end }}
{{- end }}
