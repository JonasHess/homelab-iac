{{- /*
  Per-route SecurityPolicy that opts THIS route out of the Gateway-level OIDC.

  Envoy Gateway uses full-replace precedence: a route-level SP completely
  overrides the Gateway-level SP for the routes it targets. We want to
  remove OIDC for these routes while leaving the listener-level gates
  (LAN-only L2 on port 443, mTLS on port 4443) in charge of access control,
  so the body is just an "allow all" authorization. No IP allowlist —
  that's covered at the listener layer.

  Trigger: `oauth: false` (or absent) on the route. Routes meant to be
  auth-protected set `oauth: true` and emit nothing here, inheriting the
  Gateway-level OIDC.
*/ -}}
{{- $appName := required "appName must be set in values.yaml" .Values.appName }}

{{- if .Values.ingress }}
  {{- if .Values.ingress.https }}
    {{- $gatewayNamespace := required "global.gateway.namespace must be set" $.Values.global.gateway.namespace }}

    {{- range $index, $ingress := .Values.ingress.https }}
    {{- if not $ingress.oauth }}

---
apiVersion: gateway.envoyproxy.io/v1alpha1
kind: SecurityPolicy
metadata:
  name: {{ $appName }}-securitypolicy-{{ $ingress.port }}-{{ $index }}
  namespace: {{ $gatewayNamespace }}
  annotations:
    argocd.argoproj.io/sync-options: Prune=true,SkipDryRunOnMissingResource=true
spec:
  targetRefs:
    - group: gateway.networking.k8s.io
      kind: HTTPRoute
      name: {{ $appName }}-https-{{ $ingress.port }}-{{ $index }}
  authorization:
    defaultAction: Allow
    {{- end }}
    {{- end }}
  {{- end }}
{{- end }}
