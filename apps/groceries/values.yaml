generic:
  appName: groceries
  ingress:
    https:
    - subdomain: groceries
      port: 80
      service: groceries
#    - subdomain: groceries
#      port: 3333
#      service: groceriesauth
#      matchSuffix: "&& PathPrefix(`/api`)"
#      priority: 100
#      middlewares:
#      - strip-api-prefix

  externalSecrets:
    groceries-auth-secrets:
    - COUCHDB_HMAC_KEY: /groceries/COUCHDB_HMAC_KEY
    - COUCHDB_ADMIN_USER: /groceries/COUCHDB_ADMIN_USER
    - COUCHDB_ADMIN_PASSWORD: /groceries/COUCHDB_ADMIN_PASSWORD
    - SMTP_PASSWORD: /groceries/SMTP_PASSWORD

smtp:
  host:  smtp.example.com
  from: Groceries <groceries@example.com>
  port: 587
  user: immich@example.com
